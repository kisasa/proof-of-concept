{
  "name": "specialist-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runs a backend or frontend Specialist against one story inside the specialist-sandbox ECS task. Reads its dispatch context from env vars a Temporal worker sets via ECS RunTask overrides (see src/dispatch-context.ts); everything else it fetches itself through the Linear and GitHub MCP servers, and a local git checkout, the same way the manually-dispatched specialist did.",
  "scripts": {
    "start": "node --import tsx src/run.ts",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.281"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.9"
  }
}
