# Activation: Intake Agent

PROJECT: <PROJECT_TITLE> (<PROJECT_ID>)

<ACTIVATION_TRIGGER>

Using the Linear connector, read the project's current description to find the
linked business-requirements document, then read that document itself — not
just the one-line summary. Also read: all project attachments, the full
comment thread (structured — top-level comments vs. replies, authors,
timestamps), the project's status-update history (Linear has no webhook for
project comments, so a human's reply arrives as a status update — see your
Trigger section), and the linked evidence issue's attachments.

Determine your current state from the comment thread and the status updates,
and proceed per your decision flow.
